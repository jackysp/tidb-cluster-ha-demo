# Topology file for cluster A
# TiDB version: v8.5.1

global:
  user: "tidb"
  ssh_port: 22
  deploy_dir: "/tidb-deploy"
  data_dir: "/tidb-data"

pd_servers:
  - host: 10.148.0.5

tidb_servers:
  - host: 10.148.0.5

tikv_servers:
  - host: 10.148.0.5

cdc_servers:
  - host: 10.148.0.5

monitoring_servers:
  - host: 10.148.0.5

grafana_servers:
  - host: 10.148.0.5

alertmanager_servers:
  - host: 10.148.0.5
